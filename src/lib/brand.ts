/**
 * Colores de marca para avatares generados.
 * Deben coincidir con --color-cliente y --color-pro de globals.css.
 */

/** Azul: el que busca servicios. */
export const CLIENT_BLUE = "#2563eb";

/** Verde: el que ofrece servicios. */
export const PRO_GREEN = "#059669";
